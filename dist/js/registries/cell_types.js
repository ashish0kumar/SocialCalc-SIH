import { Registry } from "../registry";
/**
 * This registry is intended to map a cell content (raw string) to
 * an instance of a cell.
 */
export const cellRegistry = new Registry();
//# sourceMappingURL=cell_types.js.map